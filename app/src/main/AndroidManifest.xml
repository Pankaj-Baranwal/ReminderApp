<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tnine.application">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="23"/>

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBT0dPGIOTmmuh5XKdzS4Adkbyj9FEHdjM"/>

        <activity
            android:name=".google.playservices.placecompleteactivity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".contentlist.ContentList"
            android:label="Reminder List">
        </activity>

        <activity android:name=".alarm.AlertActivity"
            android:label="AlertActivity">
        </activity>

        <activity android:name=".contentlist.UpdateValue"
            android:label="Update Reminder">
        </activity>

    </application>

</manifest>
